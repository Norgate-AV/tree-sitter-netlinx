if (foo())
{
    cTemp = bar()
    if (baz())
        cTemp = fizz()
}

// Did not find seperator, argument is the command (like ?SOMETHING)
else
    cCmd =  "
                'Hello, ',
                'World!'
            "
