// Simple parenthesized declarator
INTEGER (counter)

// Nested parenthesized declarator
CHAR ((message))

// Parenthesized array declarator
INTEGER (values)[10]

// Complex nested parenthesized array
CHAR ((buffer)[50])[100]
